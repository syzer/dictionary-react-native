import {
    StyleSheet
} from 'react-native'

export const styles = StyleSheet.create({
    // For the container View
    parent: {
        padding: 16
    },

    input: {
        height: 50,
        borderColor: 'gray'
    },

    // For the Text label
    germanLabel: {
        marginTop: 20,
        fontWeight: 'bold'
    },

    // For the Text meaning
    germanWord: {
        marginTop: 15,
        fontSize: 30,
        fontStyle: 'italic'
    }
})
