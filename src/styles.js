import {
    StyleSheet
} from 'react-native'

export const styles = StyleSheet.create({
    // For the container View
    parent: {
        padding: 16,
        marginTop: 10
    },
    text: {
        fontSize: 20
    },
    input: {
        height: 50,
        borderColor: 'gray',
        borderWidth: 0
    },
    // For the Text label
    germanLabel: {
        marginTop: 20,
        fontWeight: 'bold'
    },
    // For the Text meaning
    germanWord: {
        marginTop: 15,
        fontSize: 40,
        fontStyle: 'italic',
        height: 50
    },
    dogeImg: {
        marginTop: 50,
        height: 250,
        width: 320
    }
})
